
%\newcount\EqNo\EqNo=0
%\def\Eq {\global\advance\EqNo by 1 \eqno(\number\EqNo)}

\def\AdataFull {\mathcal{A}_{\rm data}^{\rm full}}
\def\AdataSB {\mathcal{A}_{\rm data}^{\rm SB}}


\def\DataFull {\mathcal{D}^{\rm full}}
\def\DataSB {\mathcal{D}^{\rm SB}}


\def\AMCFull {\mathcal{A}_{\rm MC}^{\rm full}}
\def\AMCSB {\mathcal{A}_{\rm MC}^{\rm SB}}



\def\AdataSecFull {\mathcal{A}_{\rm sec,data}^{\rm full}}
\def\AdataSecSB {\mathcal{A}_{\rm sec,data}^{\rm SB}}

\def\AMCSecFull {\mathcal{A}_{\rm sec,MC}^{\rm full}}
\def\AMCSecSB {\mathcal{A}_{\rm sec,MC}^{\rm SB}}


\def\AMCPrimFull {\mathcal{A}_{\rm prim,MC}^{\rm full}}
\def\AMCPrimSB {\mathcal{A}_{\rm prim,MC}^{\rm SB}}


\def\MCSecFull {\mathcal{S}_{MC}^{\rm full}}
\def\MCSecSB {\mathcal{S}_{MC}^{\rm SB}}


\def\MCPrimFull {\mathcal{P}_{MC}^{\rm full}}
\def\MCPrimSB {\mathcal{P}_{MC}^{\rm SB}}



\def\AMCFull {\mathcal{A}_{\rm MC}^{\rm full}}
\def\AMCSB {\mathcal{A}_{\rm MC}^{\rm SB}}

\def\Bsec {\mathcal{B}_{\rm sec}}
\def\BsecFull {\mathcal{B}_{\rm sec}^{\rm full}}
\def\BsecSB {\mathcal{B}_{\rm sec}^{\rm SB}}

\def\fpSB {f_{p}^{\rm SB}}
\def\fsSB {f_{s}^{\rm SB}}

\def\fpMCSB {f_{p,{\rm MC}}^{\rm SB}}
\def\fsMCSB {f_{s,{\rm MC}}^{\rm SB}}
\def\fpdataSB {f_{p,{\rm data}}^{\rm SB}}
\def\fsdataSB {f_{s,{\rm data}}^{\rm SB}}

\def\fpMCFull {f_{p,{\rm MC}}^{\rm full}}
\def\fsMCFull {f_{s,{\rm MC}}^{\rm full}}
\def\fpdataFull {f_{p,{\rm data}}^{\rm full}}
\def\fsdataFull {f_{s,{\rm data}}^{\rm full}}


\def\Alpgen {{\sc Alpgen}}
\def\Powheg {{\sc Powheg}}
\def\Pythia {{\sc Pythia}}
\def\Herwig {{\sc Herwig}}
\def\MCatNLO {{\sc MC{@}NLO}}

\def \Dzero {D{\O}}
\def \DZero {D{\O}}


\def\ttbar {t\bar t } 
\def\qq {$q\bar q$ }
\def\ppbar {$p\bar p$ }
\def\pp {$pp$ }
\def\ee {$e^+e^-$ }
\def \Wpm {$W^\pm$ }
\def \Wp {$W^+$ }
\def \Wm {$W^-$ }
\def \W {$W$ }
\def \Z {$Z^0$ }
\def \g {$g$ }

\def \Bpm {$B^\pm$ }
\def \Bp {$B^+$ }
\def \Bm {$B^-$ }
\def \B {$B$ }

\def \t {$t$ }
\def \b {$b$ }
\def \c {$c$ }
\def \s {$s$ }
\def \u {$u$ } 
\def \d {$d$ }
\def \q {$q$ }

\def \tbar {$\bar t$ }
\def \bbar {$\bar b$ }
\def \cbar {$\bar c$ }
\def \sbar {$\bar s$ }
\def \ubar {$\bar u$ }
\def \dbar {$\bar d$ }
\def \qbar {$\bar q$ } 

\def \e {$e$ }
\def \muon {$\mu$ }
\def \tauon {$\tau$ }
\def \l {$l$ }

\def \D0 {D\O }
\def \TeV #1{#1$\,${\rm TeV}}
\def \GeV #1{#1$\,${\rm GeV}}
\def \MeV #1{#1$\,${\rm MeV}}
\def \keV #1{#1$\,${\rm keV}}

\def \qqtt {q\bar q\rightarrow t\bar t}
\def \pptt {p\bar p\rightarrow t\bar t}
\def \ggtt {g g\rightarrow t\bar t}
\def \sqqtt {\hat\sigma_{q\bar q\rightarrow t\bar t}}
\def \spptt {\sigma_{p\bar p\rightarrow t\bar t}}
\def \sggtt {\hat\sigma_{g g\rightarrow t\bar t}}
\def \tWb {$t\!\rightarrow\!W\!+b$}

\def \Vtb {V_{tb}}
\def \Vtbsq {|V_{tb}|^2}
\def \GF {G_{F}}
\def \Ew {E_W^*}
\def \Eb {E_b^*}
\def \mwsq {m_W^2}
\def \mbsq {m_b^2}
\def \mtsq {m_t^2}

\def \sumqq {\sum\limits_{q,q'}}
\def \intx #1#2{\int\limits_{\!\!#1}^{\ #2}}
\def \intxx {\intx{0}{1}\!\!\!\intx{0}{1}}

\def \fp #1 {f^p_#1}
\def \fpb #1 {f^{\bar p}_{#1}}
\def \fpq {f^p_q}
\def \fpqb {f^p_{\bar q}}
\def \fpbq {f^{\bar p}_q}
\def \fpbqb {f^{\bar p}_{\bar q}}


\def \fb #1{#1$\,$fb}
\def \pb #1{#1$\,$pb}
\def \invfb #1{#1$\,$${\rm fb}^{-1}$}
\def \invpb #1{#1$\,$${\rm pb}^{-1}$}


\def \alphas {\alpha_s(\mu)}
\def \alphasqmu {\alpha_s^2(\mu_R)}
\def \alphasq {\alpha_s^2}
\def \th {\hat t}
\def \uh {\hat u}
\def \sh {\hat s}
\def \sqrtsh{\sqrt{\sh}}
\def \sigmacms {\der{\hat\sigma}{\!\cos\theta^*}}
\def \pf {|\vec p_f^{\,*}|}
\def \pin {|\vec p_i^{\,*}|}

\def \A {\mathcal{A}}
\def \Mfi {\mathcal{M}_{fi}}
\def \gmu {\gamma_\mu}
\def \gnu {\gamma_\nu}
\def \grho {\gamma_\rho}
\def \gsigma {\gamma_\sigma}
\def \Gmu {\gamma^\mu}
\def \Gnu {\gamma^\nu}
\def \Grho {\gamma^\rho}
\def \Gsigma {\gamma^\sigma}
\def \Tr {{\rm Tr}}

\def \Emiss {\slashed E}
\def \Et {E_T}
\def\Etmiss{\slashed{E}_{\rm T}}
\def\vecEtmiss{{\vec{\slashed{E}}}_{\rm T}}

\def\Rjet {R_{\rm jet}}
\def\vecptZ {\vec{p}_{\rm T}^{\rm \, Z}}
\def\ptjet {p_{\rm T}^{\rm jet}}
\def\ptZ {p_{\rm T}^{\rm Z}}
\def\jetdeteta{\eta_{\rm jet}^{\rm det}}


\def \Ht {$H_T$}
\def \pt {p_{\rm T}}
\def \arctanh {{\rm arctanh\ }}
\def \arctan {{\rm arctan\ }}

\def\der #1#2{\frac{{\rm d} #1}{{\rm d} #2} }
\def\dif #1{{\rm d}#1}
\def\diff #1{{\rm d}^2#1}
\def\pder #1#2{\frac{\partial #1}{\partial #2} }
\def\II #1{#1^{\mu\nu} }
\def\div #1{\frac{1}{#1} }

\def\Rjet {\mathcal{R}_{\rm jet}}
\def\Rjethat {\hat{\mathcal{R}}_{\rm jet}}
\def\Rcc {R_{\rm CC}}
\def\Feta {\mathcal{F}_{\eta}}
\def\Fetahat {\hat{\mathcal{F}}_{\eta}}
\def\Reta {\mathcal{R}_{\eta}}
\def\Retahat {\hat{\mathcal{R}}_{\eta}}
\def\kO {k_{\mathcal{O}}}
\def\kR {k_{\mathcal{R}}}
\def\kOkR {k_{\mathcal{O} / k_{\mathcal{R}}}}
\def\ktopo {k_{\rm topo}}
\def \Ejet {E_{\rm jet}}
\def \Eraw {E_{\rm raw}}
\def \Ejethat {\hat{E}_{\rm jet}}
\def \Off {\mathcal{O}}
\def\Sh {\mathcal{S}}
\def \Offhat {\hat{\mathcal{O}}}
\def\Shhat {\hat{\mathcal{S}}}

